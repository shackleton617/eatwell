<%= render 'shared/navbar_show' %>
<div class="offers-wrapper">
  <div class="container">
    <div class="row text-center">
      <h3>Redeem your
        <% if current_user.token_wallet == nil %>
          <%= "0" %> impact points
        <% else %>
          <%= current_user.token_wallet %> impact points
        <% end %>
        for delicious offers!
      </h3>
    </div>
    <div class="row card-padding-offers">
      <%= render 'offer' %>
    </div>
  </div>
</div>

<!-- home link -->


